services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    depends_on:
      - redis
  socket:
    build: .
    command: npm run socket
    ports:
      - "4000:4000"
    env_file:
      - .env
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
